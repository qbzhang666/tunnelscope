# Core API
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
PyJWT>=2.8
httpx>=0.27          # FastAPI TestClient / smoke test

# Production extras (uncomment for Postgres + migrations + OIDC RS256):
# psycopg[binary]>=3.1
# geoalchemy2>=0.14
# alembic>=1.13
# cryptography>=42        # PyJWT RS256 / JWKS verification

# The IFC-export and PDF-report endpoints reuse the parent Streamlit app's
# utils/, which pull numpy, plotly, folium, matplotlib (and rdflib/owlrl for
# reasoning). Run the backend in the same environment as the Streamlit app,
# or `pip install -r ../requirements.txt` as well.
